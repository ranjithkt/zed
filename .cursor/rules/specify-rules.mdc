# zed Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-25

## Active Technologies
- Rust 1.92 (`rust-toolchain.toml`) + GPUI (`crates/gpui`), Zed workspace/project abstractions (`crates/workspace`, `crates/project`, `crates/project_panel`, `crates/outline_panel`, `crates/editor`) (001-multi-window)
- Local filesystem + existing workspace persistence (SQLite via `sqlez`) where already used (001-multi-window)
- Rust 1.92 (see `rust-toolchain.toml`) + `gpui`, `workspace`, `title_bar`, `ui` (002-secondary-window-file-menu)
- N/A (no new persistence) (002-secondary-window-file-menu)

- Rust 1.92 (`rust-toolchain.toml`) + GPUI (`crates/gpui`), Zed workspace/project abstractions (`crates/workspace`, `crates/project`, `crates/project_panel`) (001-multi-window)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test; cargo clippy

## Code Style

Rust 1.92 (`rust-toolchain.toml`): Follow standard conventions

## Recent Changes
- 002-secondary-window-file-menu: Added Rust 1.92 (see `rust-toolchain.toml`) + `gpui`, `workspace`, `title_bar`, `ui`
- 001-multi-window: Added Rust 1.92 (`rust-toolchain.toml`) + GPUI (`crates/gpui`), Zed workspace/project abstractions (`crates/workspace`, `crates/project`, `crates/project_panel`, `crates/outline_panel`, `crates/editor`)

- 001-multi-window: Added Rust 1.92 (`rust-toolchain.toml`) + GPUI (`crates/gpui`), Zed workspace/project abstractions (`crates/workspace`, `crates/project`, `crates/project_panel`)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
